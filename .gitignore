# OS files
.DS_Store
Thumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp

# Node (only if you later add tooling)
node_modules/
npm-debug.log*

# Local env / secrets — never commit API keys (e.g. an ESV API key)
.env
.env.local
